import { CustomStateMatcher } from './custom-state-matcher';

describe('CustomStateMatcher', () => {
  it('should create an instance', () => {
    expect(new CustomStateMatcher()).toBeTruthy();
  });
});
